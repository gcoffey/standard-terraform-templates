resource "azurerm_app_service_hybrid_connection" "{{ app_service_hybrid_connection.name }}" {
  ## Required Vars
  app_service_name   = "{{ app_service_hybrid_connection.app_service_name }}"
  resource_group_name   = "{{ app_service_hybrid_connection.resource_group_name }}"
  relay_id   = "{{ app_service_hybrid_connection.relay_id }}"
  hostname   = "{{ app_service_hybrid_connection.hostname }}"
  port   = "{{ app_service_hybrid_connection.port }}"

  ## Optional Vars
  send_key_name   = "{{ app_service_hybrid_connection.send_key_name }}"

}
