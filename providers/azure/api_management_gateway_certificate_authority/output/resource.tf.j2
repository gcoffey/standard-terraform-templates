resource "azurerm_api_management_gateway_certificate_authority" "{{ api_management_gateway_certificate_authority.name }}" {
  ## Required Vars
  api_management_id   = "{{ api_management_gateway_certificate_authority.api_management_id }}"
  certificate_name   = "{{ api_management_gateway_certificate_authority.certificate_name }}"
  gateway_name   = "{{ api_management_gateway_certificate_authority.gateway_name }}"

  ## Optional Vars
  is_trusted   = {{ api_management_gateway_certificate_authority.is_trusted }}

}
